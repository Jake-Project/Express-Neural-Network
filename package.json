{
  "name": "neural_network",
  "version": "1.0.0",
  "description": "An attempt at making a neural network accessible via an Express Server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
